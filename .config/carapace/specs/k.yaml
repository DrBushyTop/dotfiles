# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: k
description: alias -> kubectl
parsing: disabled
completion:
  positionalany:
    - "$carapace.bridge.CarapaceBin([kubectl])"
